#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

# if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
#   read -p "Do you want to start X? (Y/n) " answer
#   answer=${answer:-y}
#   if [[ $answer = y || $answer = Y ]]; then
#     exec startx
#   fi
# fi

